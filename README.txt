# AI Bánh ngọt Classifier 🍩

## Cách chạy:
1. Cài thư viện:
   `pip install -r requirements.txt`

2. Chạy API backend:
   `uvicorn api:app --reload`

3. Chạy giao diện người dùng:
   `streamlit run app.py`

## Output:
- Phân loại: donut, su kem, sừng bò, tart trứng
